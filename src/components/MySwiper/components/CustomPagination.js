import {
    StyledCustomPagination,
    Pagination,
} from '../styles/CustomPagination.styled'

function CustomPagination() {
    return (
        <StyledCustomPagination
            className="my-swiper__pagination"
            // style={{ height: '2px' }}
        />
    )
}

export default CustomPagination
